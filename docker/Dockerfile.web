# Web (Flask) image
FROM python:3.13-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# System deps
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt && \
    pip install --no-cache-dir piper-tts

COPY . /app

ENV FLASK_APP=app \
    PORT=8000

# Gunicorn config via env
ENV GUNICORN_WORKERS=3 \
    GUNICORN_TIMEOUT=60

EXPOSE 8000

CMD ["bash", "-lc", "exec gunicorn -w ${GUNICORN_WORKERS} -k gthread -t ${GUNICORN_TIMEOUT} -b 0.0.0.0:8000 app:app"]

# Web (Flask) image
FROM python:3.13-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# System deps
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

COPY . /app

ENV FLASK_APP=app \
    PORT=8000

# Gunicorn config via env
ENV GUNICORN_WORKERS=3 \
    GUNICORN_TIMEOUT=60

CMD ["bash", "-lc", "exec gunicorn -w ${GUNICORN_WORKERS} -k gthread -t ${GUNICORN_TIMEOUT} -b 0.0.0.0:8000 app:app"]
